import inspection from './inspection'
import expenses from './expenses'
const driver = {
    inspection: Object.assign(inspection, inspection),
expenses: Object.assign(expenses, expenses),
}

export default driver